# React Bacon.js Browserify configuration

Browserify will automatically transpile `react-baconjs`. Just set the
`NODE_ENV` environment variable to `development` or `production` when invoking
`browserify`.
